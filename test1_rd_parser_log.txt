========================================
        递归下降解析日志
========================================

进入 <Program>
  进入 <StmtList>
    进入 <Stmt>
      进入 <ForStmt>
        进入 <DeclStmt>
          match int (int)
          match = (=)
          进入 <Expr>
            进入 <AddExpr>
              进入 <MulExpr>
                进入 <Unary>
                  进入 <Primary>
                    match NUM (0)
                  退出 <Primary>
                退出 <Unary>
              退出 <MulExpr>
            退出 <AddExpr>
          退出 <Expr>
        退出 <DeclStmt>
        进入 <Stmt>
          进入 <Block>
            进入 <StmtList>
              进入 <Stmt>
                进入 <AssignStmt>
                  进入 <AssignExpr>
                    match = (=)
                    进入 <Expr>
                      进入 <AddExpr>
                        进入 <MulExpr>
                          进入 <Unary>
                            进入 <Primary>
                              match IDENT (x)
                            退出 <Primary>
                          退出 <Unary>
                        退出 <MulExpr>
                        进入 <MulExpr>
                          进入 <Unary>
                            进入 <Primary>
                              match IDENT (i)
                            退出 <Primary>
                          退出 <Unary>
                        退出 <MulExpr>
                      退出 <AddExpr>
                    退出 <Expr>
                  退出 <AssignExpr>

【赋值语句分析表】x=x+i;
文法 | 分析串 | 分析字符 | 剩余串
S -> id = Expr ;                            x             x=x+i;
Expr -> Term ExprP          x=              x             x+i;
Term -> Factor TermP        x=              x             x+i;
Factor -> id                x=              x             x+i;
TermP -> ε                  x=x             +             +i;
ExprP -> + Term ExprP       x=x             +             +i;
Term -> Factor TermP        x=x+            i             i;
Factor -> id                x=x+            i             i;
TermP -> ε                  x=x+i           ;             ;
ExprP -> ε                  x=x+i           ;             ;

                退出 <AssignStmt>
              退出 <Stmt>
            退出 <StmtList>
          退出 <Block>
        退出 <Stmt>
      退出 <ForStmt>
    退出 <Stmt>
  退出 <StmtList>
退出 <Program>

========================================
      语义/中间代码生成日志
========================================

i = 0
L1:
t1 = i < n
ifFalse t1 goto L2
t2 = x + i
x = t2
t3 = i + 1
i = t3
goto L1
L2: